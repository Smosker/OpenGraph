from setuptools import setup, find_packages

setup(
    name='opengraph',
    version='0.1',
    description='OpenGrap library for python',
    author='Vladimir Koljasinkij',
    author_email='smosker@gmail.com',
    include_package_data=True,
    packages=find_packages(),
)
